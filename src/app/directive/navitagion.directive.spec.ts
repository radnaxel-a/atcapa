import { NavitagionDirective } from './navitagion.directive';

describe('NavitagionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavitagionDirective();
    expect(directive).toBeTruthy();
  });
});
